#include <stdio.h>
#include <stdlib.h>
#include "sort.h"

/**
 * bubble_sort - entry point
 * 
 * return: void
 */

void bubble_sort(int *array, size_t size)
{
    int i;
    int temp;

    for(; i < size; i++)
    {
        if(i > array[i])
        {
            temp = array[i]
            
        }
    }
}